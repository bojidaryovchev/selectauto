{
  "name": "@auctions-ingestion/extension",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "SelectAuto Lot Checker — browser extension that flags whether a Copart/IAAI/Encar lot is already listed on selectauto.bg and builds a Viber message.",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare",
    "type-check": "wxt prepare && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wxt": "0.21.0"
  }
}
