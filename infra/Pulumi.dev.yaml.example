# Example dev stack config. Copy to Pulumi.dev.yaml, then set secret values with
# `pulumi config set --secret ...` (do NOT commit real secrets).
#
# Secret config values below are placeholders. The real AUCTIONS_API_KEY and
# NEON_DATABASE_URL should be set as Pulumi secrets and are pushed into AWS
# Secrets Manager by the stack. See README.
config:
  aws:region: eu-central-1
  selectauto-infra:projectName: selectauto
  selectauto-infra:environment: dev
  selectauto-infra:auctionsApiBaseUrl: https://auctionsapi.com/api
  selectauto-infra:hourlySyncScheduleExpression: rate(1 hour)
  selectauto-infra:dailyReferenceSyncScheduleExpression: rate(1 day)
  selectauto-infra:logRetentionDays: "14"
  selectauto-infra:perPage: "1000"
  selectauto-infra:incrementalMinutes: "75"
  # Secrets — set these with the CLI, they will appear here encrypted:
  #   pulumi config set --secret selectauto-infra:auctionsApiKey  <key>
  #   pulumi config set --secret selectauto-infra:neonDatabaseUrl <pooled-connection-string>
  # selectauto-infra:auctionsApiKey:
  #   secure: v1:...
  # selectauto-infra:neonDatabaseUrl:
  #   secure: v1:...
